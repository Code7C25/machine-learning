# Versiones pinneadas/compatibles (ajústalas si tu entorno lo requiere)
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
scrapy>=2.11,<3.0
httpx>=0.27,<0.28
beautifulsoup4>=4.12,<5.0
scrapy-playwright>=0.0.34,<0.1
playwright>=1.47,<2.0
celery>=5.3,<6.0
redis>=5.0,<6.0

# Notas de instalación:
# 1) Después de instalar 'playwright', ejecuta:
#    python -m playwright install chromium
# 2) En Windows, instala Redis (por ejemplo desde releases de Memurai/Redis o WSL).