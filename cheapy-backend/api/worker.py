"""
DEPRECADO (LEGACY)
------------------
Este módulo pertenecía a una versión anterior del worker de Celery bajo `api/`.
El worker activo está en `worker/` (ver `worker/celery_app.py` y `worker/tasks.py`).

Se mantiene el archivo únicamente para evitar imports rotos en código histórico.
No contiene implementación funcional.
"""

# Marcador intencional para que el módulo sea inerte
pass